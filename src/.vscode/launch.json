{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "CUDA C++: Launch",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/quasarq",
            //"args": "--qubits=30 -print-final-tableau -print-gates -print-initial-tableau --depth=5"
            //"args": "--qubits=16 --M=0.2 --depth=15 -print-gates --write-circuit=2"
            //"args": "-tune-identity -tune-step -tune-reset -tune-transpose2r -tune-transpose2c -tune-allpivots -tune-multdeterminate --qubits=5000"
            "args": "-tune-copyindeterminate --qubits=8 --depth=15 --tune-initial-qubits=8 --tune-step-qubits=8"
            //"args": "--qubits=1000 --config-path=\"$HOME/quantum/github/QuaSARQ/src/kernel.config\""
            //"args": "${workspaceFolder}/q1000_d100.stim ${workspaceFolder}/q1000_d100.stim"
        },
        {
            "name": "CUDA C++: Attach",
            "type": "cuda-gdb",
            "request": "attach"
        }
    ]
}